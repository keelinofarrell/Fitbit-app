<%= form_for(@my_fitbit) do |f| %>
  <% if @my_fitbit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_fitbit.errors.count, "error") %> prohibited this my_fitbit from being saved:</h2>

      <ul>
      <% @my_fitbit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fitbit_id %><br>
    <%= f.collection_select :fitbit_id, Fitbit.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br>
    <%= f.collection_select :member_id, Member.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :steps %><br>
    <%= f.number_field :steps %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
